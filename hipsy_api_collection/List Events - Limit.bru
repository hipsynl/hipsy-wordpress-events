meta {
  name: List Events - Limit
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/organisation/{{organisationSlug}}/events
  body: none
  auth: bearer
}

params:query {
  period: all
  limit: 5
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

meta {
  name: List Events - Past
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/organisation/{{organisationSlug}}/events
  body: none
  auth: bearer
}

params:query {
  period: past
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}

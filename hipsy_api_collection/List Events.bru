meta {
  name: List Events
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/organisation/{{organisationSlug}}/events?period=all&limit=5&page=1
  body: none
  auth: bearer
}

params:query {
  period: all
  limit: 5
  page: 1
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiKey}}
}
